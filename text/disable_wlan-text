

  This menu entry disables temporary (until next reboot) or permanently (not
  recommended) TorBox's WLAN. This makes only sense and is only possible
  if your client is connected via Ethernet cable.

  How do you want to disable TorBox's WLAN (ESC -> go back)?
