The default bandwidth communicating with the Tor network is set to 1 GB (GByte per second). We do not recommend to change the default setting.
Nevertheless, with this menu entry, you can change the default setting, for example, if TorBox is used as a guest WiFi with limited capacity.

Please write your value in one of the following format: N bytes|KB|MB|GB or write default.
