With this menu entry, you can DIRECTLY EDIT a CLIENT AUTHORIZATION by starting an editor.

After changing the client authorization, use the following commands in the editor:
CTRL-S to save the changes.
CTRL-X to exit the editor.

Would you like to change a specific client authorization directly?
